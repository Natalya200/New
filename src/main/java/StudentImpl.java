import java.util.List;

public class StudentImpl extends Student {
    public StudentImpl(String name, String group, int course, List<Integer> grades) {
        super(name, group, course, grades);
    }
}
